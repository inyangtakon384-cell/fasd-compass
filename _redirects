# FASD Compass — Cloudflare Pages redirect rules

# www → apex (canonical URL)
https://www.myfasdguide.org/* https://myfasdguide.org/:splat 301!

# SPA fallback — all routes serve index.html
/*  /index.html  200
